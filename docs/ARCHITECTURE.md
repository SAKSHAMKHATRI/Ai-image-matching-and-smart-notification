# Phase 0 Architecture Boundary

The repository currently contains two independently runnable applications:

- `frontend/`: React, Vite, and TypeScript client
- `backend/`: FastAPI service with the Phase 0 `GET /health` endpoint

Authentication, profiles, SQLite application data, storage, AI, matching, claims, notifications, and administration belong to later phases and are intentionally absent from this foundation.
